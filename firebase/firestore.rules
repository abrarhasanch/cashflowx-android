rules_version = '2';
service cloud.firestore {
  // Simple rules to unblock login/data access: any authenticated user can read/write.
  // TODO: tighten these back down once auth issues are resolved.
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
